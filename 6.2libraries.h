#ifndef LIBRARIES_H // To make sure you don't declare the function more than once by including the header multiple times.
#define LIBRARIES_H

int square(int num);

double power(double base, int exponent);

void changeVal(int *num);

int calOldest(int ages[], int size);

#endif // To end the if statement from above